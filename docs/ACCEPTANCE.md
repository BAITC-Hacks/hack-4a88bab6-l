# Проверки Career Quest

Статусы ниже фиксируют только реально проведённые проверки. `Ожидает` означает, что результат нельзя считать подтверждённым.

| Проверка | Команда или сценарий | Статус |
|---|---|---|
| Схема исходного ZIP и контрольные числа | Чтение четырёх файлов и README датасета | Пройдено: 200 сотрудников, 40 мероприятий, 60 навыков, 32 профиля роли/грейда, 2 743 записи участия. |
| Replay, допуск, Skip, архив, импорт, доступы, сбои AI | `python -m pytest` | Пройдено: 23 теста, 11,60 с в последнем полном прогоне backend; 3 предупреждения deprecation. |
| Типы frontend | `cd frontend && npm run typecheck` | Пройдено: финальный frontend. |
| Сборка frontend | `npm run build`, затем `npm run build` в Docker | Пройдено: локально и в Docker `tsc -b` и Vite build. Есть предупреждение о JS chunk 650 кБ; сборку оно не блокирует. |
| Аудит npm-зависимостей | `npm audit` и `npm ci` в Docker | Пройдено: 0 уязвимостей в проверенном наборе пакетов. |
| Запуск контейнера | `APP_PORT=8013 docker compose up --build -d` | Пройдено после финальной правки frontend: образ собран, контейнер Up на 8013, `GET /login` вернул HTTP 200. |
| Сохранение SQLite после перезапуска | Цель E0005 Senior → Lead; `docker compose restart career-quest`; повторный вход и чтение профиля | Пройдено: после рестарта цель осталась Lead; затем возвращена к Senior. |
| HTTP вход и профиль | `POST /api/login`, `GET /api/me`, `GET /api/employee/profile` на 8013 | Пройдено: роль employee, ID E0005, профиль E0005, 19 записей истории. |
| HTTP после финальной сборки | Вход employee/HR, профиль E0005 и `GET /api/hr/competencies` на 8013 | Пройдено: 131/44 мс на вход, 10 мс профиль, 415 мс компетенции и 60 навыков. Время измерено локально одним запросом каждого типа. |
| Рендер экранов в браузере | Headless Edge/Playwright: вход, employee, HR E0005, HR competencies | Пройдено: все четыре экрана открылись, компетенции загрузились, JS page errors нет. В профиле employee и HR по четыре видимых SVG-пути радара с данными. |
| Основные действия в браузере | Headless Edge/Playwright на 8013: вход → AI → Skip → альтернатива → возврат → HR архив/восстановление → preview импорта | Пройдено для перечисленного: E0005 получил AI-подбор HTTP 200; Skip `EV_007` сохранил покрытие 68%, повторный подбор дал `EV_040`/`EV_005`/`EV_036` без пропущенного, Skip снят. HR получил честно помеченный fallback, архивировал `EV_007` с причиной и восстановил. Preview корректного синтетического профиля показал 1 нового сотрудника, 0 ошибок, HTTP 200; commit не нажимали. Ошибок JavaScript не было. Выполнение активности через браузер в этом smoke не проверялось; его логика проверена API/pytest. |
| Live-вызов OpenAI | Отдельно с личным серверным ключом | Пройдено для одного вызова: `scripts/live_openai_check.py --employee E0005` вернул `validated_ai`, `gpt-4.1-mini`, 5,79 с, `EV_040`/`EV_036`. Один предыдущий вызов скрипта дал `ValueError` за 4,38 с; два более ранних запроса были timeout. Позднее локальный `generate` также вернул `source=ai` с тремя событиями, но без измеренной задержки. Подробности в [MODEL_SELECTION.md](MODEL_SELECTION.md). |
| Скан секретов | Локальная проверка 24 файлов | Пройдено: 0 найденных утечек по применённым правилам скана. |

Ориентиры ТЗ: отклик интерфейса до 2 секунд, получение AI-рекомендации до 10 секунд. Они требуют измерения в запущенной среде. Любой fallback должен быть видимо отмечен как резервный подбор.
